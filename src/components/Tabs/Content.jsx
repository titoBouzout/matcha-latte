/**
 * Passthrough for content in TabPanel
 * @module Tabs/Content
 * @param {object} props Leftover props are passed to the section container
 * @param {boolean} props.collapse Hide content instead of not rendering
 * @param {Children} [props.children]
 * @returns {object} props
 * @url https://pota.quack.uy/Components/Tabs
 */
export default function TabContent(props) {
	return props
}
